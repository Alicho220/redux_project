import './App.css';
// import data from './data.json'

function App() {
  return (
    <div className="App">
      <h2>love</h2>
     {/* <div className="posts">
     {data.map(postData=>{
       return(
         <div key={postData}>
         <h1>{postData.title}</h1>
         <p>{postData.content}</p>
         </div>
       )
     })}

     </div> */}
    </div>
  );
}

export default App;
